/target
**/target
Cargo.lock
.idea/
.run/
.DS_Store
*.profraw
cobertura.xml
tarpaulin-report.html

# Local agent working assets (not published repo content)
.agents/
.claude/
rules/
CLAUDE.md
AGENTS.md

# NOTE: docs/ is deliberately NOT ignored. docs/SPEC.md and docs/spec/* are the
# citation trail that backs the clean-room rewrite and the MIT relicensing —
# they are published repo content.
